torch==2.3.1+cpu
onnx>=1.16
onnxruntime>=1.18
numpy>=1.26
pandas>=2.2
scipy>=1.15
fabric>=3.2
pyyaml>=6.0
tqdm>=4.66
pymoo>=0.6.1  # CP 3.2 NSGA-II search (search/evolution.py); CPU-only; pulls numpy/scipy (already pinned) + matplotlib
botorch>=0.11  # CP 3.3 BO surrogate (search/bo.py); MUST resolve under the torch==2.3.1+cpu pin above — that exact pin is the constraint, so botorch never upgrades torch (tested: botorch 0.17.2 / gpytorch 1.15.2). Pulls gpytorch/linear_operator/pyro-ppl/scikit-learn (CPU).
gpytorch>=1.12  # CP 3.3 GP kernels: CategoricalKernel(ks/e) + Matérn(depth) via MixedSingleTaskGP (transitive through botorch; pinned for clarity)
