# Cross-session memory for the NAS execution plan.
# See PROJECT_PLAN.md for the full checkpoint catalog.
# 2026-07-23 RESEARCH PROGRAMME COMPLETE — the 2026-07-15 pivot's two workstreams both CLOSED
# WON: (WS1) BEAT-N on the Orin (champion s39d_cap_a_rl, 2026-07-19, a Pareto TRADE) + (WS2)
# Phase 10 MCU retarget (champion a5fddcc, MCU-native search WON 2026-07-20, DOMINATION
# CONFIRMED ON REAL GAP8 SILICON 2026-07-23). Terminal result = two-device hardware-conditional
# NAS (Orin trade + GAP8 domination), measured at both ends. The TFM write-up lives in a
# SEPARATE repo (user, 2026-07-23). Only two non-blocking in-repo riders remain: persist
# a5fddcc's champion weights + one real-weights GAP8 run. Read the notes tail + procedure.md
# "CP 10.3 SILICON" for the full terminal picture.
current_checkpoint: "10.3"  # TERMINAL 2026-07-23 — research programme COMPLETE (see header + the notes tail "CP 10.3 SILICON"). CP 10.3 = the MCU-native search (telescoped the plan's Phase-10 10.1→10.4), WON 2026-07-20 + silicon-confirmed 2026-07-23: a5fddcc Pareto-DOMINATES the deployed baseline on real GAP8 (15.0% fewer cluster cyc = 6× the sim's 2.4% edge; 0.6299 vs 0.6227 mAP50-95; 2.3× fewer params) — state/winner_mcu/winner.json. Nothing numbered comes after: the TFM write-up is in a SEPARATE repo; old Orin Phases 7–9 (recipe-parity/KD/TRT bundle) were SUPERSEDED by the 2026-07-15 NAS-only pivot (KD tried→null, recipe-lite=the +5pt lever; no shipped TRT bundle); two non-blocking riders remain (persist a5fddcc weights + one real-weights GAP8 run). ── Prior numbered-path note (historical): CP 5.2 CLOSED 2026-07-07 (V3 PAN nano-neck wins the ablation: 0.6287±0.0078 vs control 0.6027, +0.026 — fusion is the lever, the adapter-init prior is an honest null; see procedure.md "CP 5.2 CLOSED"). STAGE 0 COMPLETE same day — THE HONEST E2E TRUTH (procedure.md "STAGE 0 COMPLETE"): baseline re-check 12.75ms fp32 (matches the anchor exactly); winner-v1 e2e 17.69ms fp32 (+38.7% SLOWER — the "12% faster" headline is RETIRED); backbone alone 13.85 vs 11.208 sum -> the @224 calibration INVERTS at 640 (x1.236, DRAM-bound); pose adapter+head offset 3.84ms (data/pose_stem_head_offset.json); fp16 deploy precision WIDENS the gap (baseline 7.58 = 1.68x, winner 12.37 = only 1.43x, +63% — depthwise vs tensor cores; cost.py's R4.3 note predicted this). No LUT-frontier arch can beat the baseline e2e (fastest sum 10.15 -> 16.11 measured; fallbacks benched + stamped in winner.json's additive e2e block, winner_beats_baseline_e2e=false). THE GOOD NUMBER: V3 fp16 = 12.75ms = 78 FPS, MEETS the 60-FPS (16.7ms) deployment bar with 24% headroom. NEXT = user decision on the thesis framing (accuracy-first at 60-FPS-deployable latency + transfer-gap findings chapter vs alternatives), then CP 5.3 AGX full-FTs of V3/V2 (jetson deploy.sh MODE=full_finetune, FT_* selectors ready). LUT-summed latencies are RANKING-ONLY forever.
last_completed: "10.3"      # MCU-native search WON (2026-07-20) + GAP8 silicon domination CONFIRMED (2026-07-23); Orin arm = BEAT-N WON (2026-07-19, s39d_cap_a_rl). Two-device hardware-conditional NAS, delivered on measurement. ── Prior (historical): CP 5.2 graft-interface ablation — V3 (PAN nano-neck) wins: 0.6287±0.0078 proxy, +0.026 over the neck-less control
completed:
  - "0"      # Phase 0 — Jetson LUT (PROJECT_PLAN.md:47)
  - "1.1"    # Skeleton repo + deps
  - "1.2"    # OFA checkpoint download + cache
  - "1.3"    # Subnet sampler (arch_dict -> nn.Module)
  - "1.4"    # ImageNet sanity (rank fidelity): Spearman rho=0.919, p=1.1e-08, 20 archs
  - "2.1"    # Arch -> block list translator (search/arch_to_blocks.py)
  - "2.2"    # LUT composite-cost + depth-binned additivity DoD (PASS) + latency calibration
  - "2.4"    # Proxy-rank fidelity: warm-head proxy ρ=0.77/regret 0 + zero-cost cross-check (reframe gate)
  - "3.1"    # Search-space encoder (search/space.py): decode(encode(arch))==arch 100/100 (CPU)
  - "3.2"    # NSGA-II evolutionary baseline (search/evolution.py): 11 non-dominated points (pymoo, CPU)
  - "3.3"    # Bayesian Optimization (search/bo.py): warm-head BO-vs-random hypervolume DoD PASS (5-seed @640; BO 3.44 vs random 2.09 HV)
  - "3.4"    # TPE fallback (search/tpe.py, Optuna MOTPE): warm-head TPE-vs-random hypervolume DoD PASS (5-seed @640; TPE 3.41 vs random 2.09 HV; reproduces BO within 0.8%)
  - "3.5"    # Winner-v1 export (search/denoise.py): de-noised knee d=[2,2,4,3,3] @ 11.208ms, 12% faster than yolo11n; reproduce-DoD PASS (|cached-mean|=0.014<0.020) after correcting a single-seed winner's curse → PHASE 3 COMPLETE
  - "4.1"    # Net2Wider (net2net/wider.py): function-preserving widen (conv/linear/BN), DoD 1e-5
  - "4.2"    # Net2Deeper (net2net/deeper.py): identity conv/linear inserts, forward unchanged
  - "4.3"    # BN handling (net2net/bn.py): re-estimate-then-invert decision; DoD 1e-3 (met at 1e-5)
  - "4.4"    # Graft-seam adapter init (net2net/graft_init.py): identity-embedding prior, wired into build_grafted_pose_model(adapter_init=) → PHASE 4 COMPLETE
  - "5.1"    # Variant library (detect/neck.py + GraftedPoseModel(neck=)): zero-gated nano-neck, identity at init, model[-1]==head preserved
  - "6.1"    # DepGraph pruning harness (prune/prune_graft.py): CPU-smoke DoD PASS — closed OUT OF ORDER 2026-07-06 while CP 5.2's GPU run is on Kaggle (current_checkpoint stays on the critical path; CP 1.4 precedent)
  - "5.2"    # Graft-interface ablation (eval/graft_ablate.py, Kaggle T4): V3 PAN nano-neck 0.6287±0.0078 vs control 0.6027±0.0131 (+0.026); adapter-init prior = honest null; gates opened (g43~0.39)
  # ── Post-CP-5.2 milestones (the 2026-07-15 pivot telescoped the numbered path; these are the
  #    artifact-backed closes after 5.2, in the project's own labels — full detail in the notes tail) ──
  - "Stage 0" # E2E honest truth (2026-07-07): winner-v1 +38.7% SLOWER e2e → the "12% faster" headline RETIRED; V3 PAN fp16 12.75ms = 78 FPS meets the 60-FPS bar (procedure.md "STAGE 0 COMPLETE")
  - "3b"      # Honest-ceiling re-search CLOSED (2026-07-07): feasible band tops at proxy 0.4965 → the null trade CONFIRMED
  - "3c"      # Dense-family scaling arm CLOSED (2026-07-08): 3-point width curve + measured cross-family verdict — latency, not accuracy, separates the four families
  - "6.2"     # Structured-pruning ladders CLOSED (2026-07-08→12): graft (6.2-G) 0.816/0.759, baseline (6.2-B) 0.834/0.790/0.809, technique table (6.2-T/6.4, HALP-lite champ); every graft strictly dominated on the measured frontier
  - "6.3"     # Pruning-as-search program CLOSED (2026-07-13, WAVE-2): per-stage width search CONVERGES to the dense Pareto boundary {r20..w22..w25} — no dominator; a valid negative result
  - "beat-n"  # BEAT-N WON (2026-07-19): Orin champion s39d_cap_a_rl 0.8721±0.0051 @ measured 11.59/6.765 ms — beats baseline latency 9.0/12.7% within 0.5pt (state/winner_beatn/winner.json)
  - "10.1"    # GAP8 toolchain bring-up + feasibility CLOSED (2026-07-16): both families compile through NNTool/AutoTiler/GVSOC; the pruned graft is 1.94× faster than baseline on GAP8
  - "10.3"    # MCU-NATIVE SEARCH WON (2026-07-20) + SILICON-CONFIRMED (2026-07-23) → TERMINAL: a5fddcc Pareto-DOMINATES baseline on real GAP8 (55.97M vs 65.83M cluster cyc = 15.0% faster, 6× the sim's 2.4%; 0.6299 vs 0.6227 mAP; 2.3× fewer params) — state/winner_mcu/winner.json
  # Old Orin Phases 7 (recipe-parity) / 8 (KD) / 9 (TRT bundle): SUPERSEDED by the 2026-07-15
  # NAS-only pivot, not formally closed — KD was tried (beat-n K arm → null-to-negative) and
  # recipe-lite was the +5pt lever (beat-n R arm); a shipped TRT deployment bundle was out of scope.
# NOTE: CP 1.4 (ImageNet sanity) CLOSED 2026-06-18 — backfilled out of order.
# Re-framed from an absolute-bar DoD to a rank-fidelity gate (the OFA accuracy
# predictor is a ranking model with a ~constant absolute offset). Spearman
# rho=0.919 (>= 0.85) across max/min corners + 18 random archs on full 50k val;
# affine fit measured~=1.105*pred-14.469 cuts MAPE 7.89%->0.38%. The max corner
# at 77.3% top-1 confirms the OFA weight load + BN recalibration are intact.
# See procedure.md "CP 1.4 CLOSED". current_checkpoint stays 2.4 (critical path).
cached_artifacts:
  ofa_mbv3_w1_0:
    net_id: "ofa_mbv3_d234_e346_k357_w1.0"
    path: ".cache/ofa/ofa_mbv3_d234_e346_k357_w1.0"  # project-relative; see "Cache relocation" 2026-06-16
    size_bytes: 31011816
    sha256: "a7def36bb4e4c688c16d37eb60d5d34b2e6dcf6438c05bc86dea918fda04c6c7"
    pinned_in: "supernet/download_ofa.py"
notes: |
  Phase 0 (Jetson LUT), CP 1.1 (skeleton + deps), CP 1.2 (OFA
  checkpoint download + hash pin), and CP 1.3 (subnet sampler) are
  complete. `python -m supernet.sampler` materialises a random
  subnet from the cached MBv3-w1.0 checkpoint and forwards
  (1, 3, 224, 224) → (1, 1000); param counts span ~3.4M (min arch)
  to ~7.7M (max arch).

  CP 1.3 added `Pillow` and `torchvision` to `requirements-nas.txt`
  (transitive deps of the elastic_nn import chain via
  `ofa.utils.my_dataloader`). CP 1.2's "what's next" guess
  (gdown + Pillow) was wrong: gdown is only needed by
  `ofa.model_zoo`, which we deliberately bypass — see
  `supernet/README.md`.

  **Venv-refresh gotcha:** a plain
  `pip install -r requirements-nas.txt` lands on PyPI's CUDA-13
  torchvision wheel and breaks at import with `libcudart.so.13: not
  found` (torch is `+cu128`). Always install via
  `bash scripts/setup_laptop_nas.sh`, which passes
  `--extra-index-url https://download.pytorch.org/whl/cu128` so the
  matching `torchvision==0.26.0+cu128` wheel is selected.

  **CP 2.1 done (2026-06-06):** `search/arch_to_blocks.py` translates
  an OFA arch_dict into the ordered `(mbconv, cfg, input_shape)` list
  the LUT is keyed on. Found that the LUT's generic MBConv widths
  didn't cover OFA's real blocks, so added `catalog/ofa_mbv3.py` (the
  shared macro-topology + `reachable_mbconv_configs()` → 91 configs)
  and unioned those into the catalog grid (mbconv 2016→2107).
  Regenerated the dummy LUT (2710 rows). DoD passes for 10 random
  archs and one real `supernet.sampler` arch (0 missing keys). See
  `procedure.md` CP 2.1.

  CP 1.4 — ImageNet sanity — CLOSED 2026-06-18 (see the CP 1.4 note at
  the end of this block and procedure.md "CP 1.4 CLOSED"). Re-framed from
  the original "within 1.5 % of OFA's published number" wording (no clean
  absolute anchor exists — OFA only publishes fine-tuned numbers) to a
  rank-fidelity gate: Spearman rho=0.919 over 20 archs. Ran on Kaggle GPU,
  independent of D1.

  **Repo reorganized 2026-06-06** (LUT-only → full project):
  `bench/`→`lut/bench/`, `export/`→`lut/export/`,
  `orchestrate/`→`lut/orchestrate/`, `docs/`→`lut/docs/`, and
  `ofa_extractor/`→`supernet/` (the latter renamed to avoid shadowing
  the pip-installed `ofa` package). Added `CLAUDE.md`. Module
  invocations are now `python -m lut.orchestrate.*` and
  `python -m supernet.*`.

  **Known blocker (CUDA):** `torch.cuda.is_available()` is False on
  this machine and `nvidia-smi` is not on PATH (unchanged since
  CP 1.1). Fine-tuning from CP 2.4 onward needs a working CUDA
  driver. Near-term checkpoints (1.4 inference-only, 2.1, 2.2) do
  NOT need CUDA. Deferred — resolve before CP 2.4.

  **Plan extended 2026-06-10:** added **Phase 8 — Knowledge
  Distillation** (distill the search winner α* against an external SOTA
  teacher → final deployable weights); the former Phase 8 (Deployment
  Packaging) is renumbered to **Phase 9**. Decisions taken via
  AskUserQuestion: (1) distillation is a standalone phase placed
  *before* deployment (ML-correct: deploy the distilled weights), not
  after; (2) the teacher is an external SOTA pretrained model, chosen at
  CP 8.1 to match D1. Roadmap change only — `current_checkpoint` /
  `completed` are unchanged (still CP 2.1); the new phase is future work
  gated on the same CUDA blocker as CP 2.4+. See `PROJECT_PLAN.md`
  Phase 8 and the plan-amendment entry in `procedure.md`.

  **Hardening pass 2026-06-11:** quality/infra only — no checkpoint
  advance (still CP 2.1). Added tests/ (102 tests; the golden row_key
  hashes in tests/test_row_key.py pin the LUT key contract — never
  update without a procedure.md decision), pyproject.toml +
  scripts/check.sh + requirements-dev.txt + GitHub Actions CI,
  lut/loader.py + precision-aware completed_keys() (CP 2.2 groundwork),
  catalog/flops.py + catalog/contracts.py, validate_arch_dict + config
  validation. Git history was REWRITTEN (credentials scrubbed) and
  force-pushed — stale clones must be re-cloned; rotate the Jetson
  password. Dummy LUT regenerated (keys identical; rows now carry
  source=roofline_dummy). TODO(user): the load_device_info failure
  policy in lut/orchestrate/run_sweep.py. See procedure.md
  "Hardening pass".

  **Measurement audit 2026-06-12:** no checkpoint change (still CP 2.1), but
  Phase 0's artifact status changed: REAL Jetson collection started —
  data/lut.jsonl now holds measured fp32/TRT-10.3 rows (3 so far), the
  complete dummy moved to data/lut.jsonl.dummy.bak. Collection hardening:
  sweep-start preflight (re-probe + clocks_locked/power-mode verification;
  --skip-preflight bypass), min_window_s duration-based sampling, persistent
  TRT timing cache, config.local.yaml overlay for the real endpoint
  (config.yaml is now a placeholder template). Decisions recorded in
  procedure.md "Measurement audit": fp32 rows deliberately allow TF32;
  peak_mem_mib = TRT scratch + IO (non-additive across blocks). The full
  sweep (~2710 rows) is the next Phase-0 action; run it before the CP 2.2
  additivity validation.

  **CP 2.2 code complete 2026-06-13:** `search/cost.py` composes a subnet's
  predicted cost from per-block LUT rows — SUM latency/params/flops, MAX
  peak_mem_mib (blocks run sequentially and free scratch; resolves the whole-net
  memory-model decision the Measurement audit deferred). Stem/head offset is
  parameterized, default no-op — it's constant across all archs (last stage
  always → 160 ch), so it's ranking-neutral; absolute value plugs in
  post-sweep. Added `CostDict`/`CostOffset` contracts + `tests/test_cost.py`
  (8 pass, 1 skips on the partial LUT). Built TDD; `check.sh` green (122 pytest).
  `current_checkpoint` is 2.2 but `last_completed` stays 2.1: the DoD
  (measured-vs-summed within 15 %) needs the full real fp32 LUT + a full-subnet
  Jetson measurement — deferred to post-sweep. See procedure.md "CP 2.2".

  **Review-response pass 2026-06-14:** no checkpoint advance (still CP 2.2 /
  last_completed 2.1) — design hardening from peer_review_simulation.md, scoped to
  findings on done code (the thesis LaTeX is not in this repo). Fixed a real gap:
  CP 2.2's `cost()` omitted resident weights from memory (contradicted
  schema.md:53-62) — added `search.cost.resident_mem_mib`. Added
  `search/validate_additivity.py` (additivity error binned by depth — the CP 2.3
  trigger; exposes the cross-block-fusion threat instead of averaging it). Plan
  gates added (PROJECT_PLAN.md): proxy-rank Kendall-τ≥0.7 DoD beside CP 2.4,
  ≥5-seed hypervolume protocol for Phase 3, depth-binned CP 2.2 DoD + pre-registered
  CP 2.3 trigger. D1 still open (gates the new CP 2.4 DoD). check.sh green (128
  passed). See procedure.md "Review-response pass".

  **Phase 0 LUT COMPLETE + offline cost preview 2026-06-16:** the real Jetson
  sweep finished — data/lut.jsonl is all 2710 rows (source=jetson_trt, fp32,
  clocks_locked). No checkpoint change (current_checkpoint stays 2.2,
  last_completed stays 2.1). Added two offline tools (.venv, CPU):
  search/cost_preview.py — costs N sampled archs from the LUT and reports proxy-vs-
  measured rank agreement (FLOPs~latency Kendall-tau 0.81, params~latency 0.37, up
  to 1.45x latency spread at near-equal FLOPs => the measured LUT beats a proxy near
  the frontier); writes data/cost_preview.csv. search/additivity_preview.py —
  `manifest` writes data/additivity_subnets.json (one subnet per depth 11..21 with
  summed_ms computed + measured_ms:null), `report` runs the depth-binned DoD from a
  filled manifest. CP 2.2's DoD is STILL OPEN: its measured-vs-summed gate needs
  whole-subnet Jetson runs (the measured_ms side; reuses run_sweep.run_remote_bench,
  only a subnet->ONNX export helper is missing). +16 tests
  (tests/test_cost_preview.py); pandas added to mypy ignore_missing_imports;
  check.sh green (146 passed). See procedure.md "CP 2.2 offline cost preview +
  additivity wiring".

  **CP 2.2 CLOSED 2026-06-17 — additivity DoD PASS + predictor calibrated.**
  current_checkpoint 2.2→2.4, last_completed 2.1→2.2, completed += "2.2". Measured
  side collected on-device: 33 whole subnets (3/depth, 11..21 blocks), each built from
  the SAME catalog blocks the per-block LUT timed (search/export_subnet.py) so
  measured−summed isolates only cross-seam TRT fusion. Depth-binned DoD PASSES — every
  bin +6.8%..+9.2% (aggregate +7.9%), well under the 15% bar, bias positive & flat in
  depth (fusion = near-constant discount, not depth-exploding) → pre-registered CP 2.3
  (residual correction) NOT triggered, skipped. Predictor fidelity
  (search/predictor_stats.py, scipy): Spearman rho 0.991, Kendall tau-b 0.943, Pearson
  r 0.998 → ranking essentially exact (search faithful). Calibration
  measured≈0.9343*summed−0.0225 ms (R²=0.996; through-origin fusion discount 0.928,
  ~7.2% faster than the sum) cuts MAPE 7.85%→1.04%. Wired opt-in into
  search.cost.cost(..., calibration=) (default identity, ranking-neutral; applied to
  the backbone sum before the stem/head offset); fit persisted to
  data/latency_calibration.json (load_latency_calibration), pairs in
  data/additivity_pairs.csv. Stem/head offset measured too (data/stem_head_offset.json:
  0.388 ms). scipy>=1.15 added to requirements.txt + pyproject mypy override (no
  torch/numpy pin drift; user-authorized). +14 tests (TDD); check.sh green (177
  passed). CP 2.4 (eval/fine-tune) next but BLOCKED on CUDA + dataset decision D1.
  See procedure.md "CP 2.2 closed".

  **D1 RESOLVED 2026-06-18 — target = gate-pose (pivot to detection/pose).** No checkpoint
  advance (current_checkpoint stays 2.4; D1 was the non-CUDA half of CP 2.4's block, now
  cleared). Target = dataset/ (Ultralytics YOLO-pose: 1 class `gate`, 8 keypoints, 2842
  train/140 val synthetic A2RL renders; see dataset/SCHEMA.md). User decisions
  (AskUserQuestion): (1) OFA subnet = BACKBONE under a YOLO11-pose head — keeps
  supernet/LUT/Net2Net/BO, ImageNet pretrain = warm-start; (2) baseline-to-beat + Phase-8
  teacher = the deployed yolo11n-pose / a bigger yolo11-pose (reuse
  yolo-ros2-inference/scripts/yolo_distillation.py), replacing MobileNetV3/ImageNet; (3)
  accuracy metric top-1 → pose mAP (OKS), reusing Ultralytics' validator. Prototype built +
  CPU-verified in .venv-nas: supernet/pose_backbone.py (OFA subnet → P3/P4/P5 at the invariant
  (40,112,160) ch, strides 8/16/32 — confirmed on a real sampled subnet @640) and detect/
  (ChannelAdapter 40/112/160→64/128/256 + a real ultralytics Pose head → boxes/scores/kpts,
  kpts=24=8×3, 8400 anchors). Added ultralytics>=8.3 (requirements-nas.txt). Fixed
  setup_laptop_nas.sh: the repo was MOVED (…/lookup_table→…/TFM_NAS) so the checked-in
  `activate` exports a stale VIRTUAL_ENV → bare python hit the system (PEP 668); now invokes
  .venv-nas/bin/python directly + clears ROS's PYTHONPATH. dataset/ gitignored (schema kept).
  check.sh green (209 passed, 1 skip). CONSEQUENCE: LUT rows are keyed per-block at res 224;
  pose runs @640 → blocks re-key → a 2nd LUT sweep at the deploy resolution is owed (append-only
  schema absorbs it; cost.py offset generalizes stem+pose-head). CP 2.4 still CUDA-gated for the
  fine-tune itself. See procedure.md "D1 resolved — pose pivot".

  **CP 2.4 CPU slice BUILT 2026-06-18.** No checkpoint advance (current_checkpoint stays 2.4,
  last_completed 2.2, completed unchanged) — the DoDs need the GPU fine-tunes. The forward-only
  graft is now TRAINABLE + eval-able end-to-end: detect.pose_model.GraftedPoseModel (subclass of
  ultralytics PoseModel; overrides only construction + _predict_once, inherits loss/v8PoseLoss/
  _apply) + build_grafted_pose_model factory; detect.evaluate.pose_map_model (grafted-model OKS
  eval via PoseValidator — ran the full 140-img val on CPU); eval/shortft.py harness (real pose
  dataloader→loss→backward→step, CPU-smoked) + the two DoD gates rank_fidelity (Kendall-τ≥0.7,
  scipy) & reproducible (twice within 0.5 pts). Built TDD; tests/test_shortft.py (10, .venv) +
  tests/test_grafted_pose_model.py (6, .venv-nas, stub backbone → loss/grad path, no checkpoint
  needed). Plus the one-command GPU driver eval/proxy_rank.py (sample archs→proxy+full→verdict,
  resumable to data/cp24_proxy_rank.json, exit 0 ⇔ DoD pass; +7 .venv tests). check.sh fast lane
  green: 226 passed, 2 skipped, 3 deselected; ruff+mypy clean. REMAINS (GPU): the ~5-epoch
  fine-tune + both DoDs (reproducibility within 0.5%; proxy-rank Kendall-τ≥0.7 over ~8–12 archs)
  → run `python -m eval.proxy_rank --device cuda` on Kaggle/Jetson. See procedure.md "CP 2.4 — CPU slice".

  **CP 2.4 GPU run FAILED both DoDs 2026-06-21 — diagnose-first.** No checkpoint advance
  (current_checkpoint stays 2.4, last_completed 2.2). The Kaggle run
  (`python -m eval.proxy_rank`, data/cp24_proxy_rank.json) gave proxy-rank Kendall-τ=0.20 (gate
  ≥0.7) and reproducibility Δ=0.0149 (≤0.005) — both FAIL. The 10 archs' full-train mAPs cluster in
  0.823–0.850, so the 5-epoch proxy only resolves "min corner is worst"; proxy/full are
  uncorrelated among the rest (random pose head + 5 epochs too noisy). DECISIONS (AskUserQuestion):
  (Q1) diagnose first — measure the FULL-train noise floor before repairing the proxy; (Q2) defer
  the head warm-start until then. Built the diagnostic in eval/proxy_rank.py: full_noise_verdict
  (pure, TDD'd — SNR=cluster-spread/noise-floor → discriminates≥2 / flat≤1 / ambiguous),
  ArchResult.full_map_reseed (back-compat), run_full_diagnostic + --diagnose-full/--indices CLI
  (reruns ~3 clustered archs at seed+1, reuses the existing full maps, resumable). `.venv-nas` is
  NOT built on this laptop → the orchestration is covered by stubbed-fine-tune tests; the real
  reseed runs on Kaggle: `python -m eval.proxy_rank --diagnose-full --indices 7,4,8 --full-epochs
  100 --device cuda`. check.sh fast lane green (236 passed). NEXT: discriminates→repair the proxy
  (revisit Q2 + epochs/LR); flat→reframe (accuracy=constraint, latency=objective; touches D4→ask
  user); ambiguous→tighten the full reference too. See procedure.md "CP 2.4 — GPU run failed".

  **CP 2.4 REPAIR: head warm-start + freeze BUILT 2026-06-21.** No checkpoint advance
  (current_checkpoint stays 2.4, last_completed 2.2). Before the 300-epoch diagnostic, a read-only
  investigation (no GPU; correlated the 10 archs vs zero-cost LUT descriptors) showed the ground
  truth is REAL not flat — full_map tracks size (depth Kendall-τ=0.767, Jetson latency τ=0.733; both
  pass the gate and stay ordered inside the cluster, τ≈0.60 on the 8 randoms) — while the 5-epoch
  proxy correlates with nothing (τ=0.20; −0.08 w/o the min corner; τ=0.07 vs FLOPs). Root cause =
  the randomly-initialized Pose head (idx8 = 2nd-best full-train but worst proxy; residual above the
  size trend ≈0.010 mAP). User chose FIX-HEAD-FIRST (AskUserQuestion). Built: detect.pose_model
  warm_start_head (shape-aware partial state_dict copy — gate donor → whole head, COCO → kpt branch
  reinit) / freeze_module / _donor_head_state; build_grafted_pose_model(head_weights=, freeze_head=);
  short_finetune trains only requires_grad params; eval.proxy_rank run_protocol gains
  head_weights/freeze_head/reset_proxy + CLI --head-weights/--freeze-head/--reset-proxy (load_supernet
  import made lazy, parity w/ run_full_diagnostic). TDD: tests/test_warm_start.py (5, .venv) + a
  reset-proxy orchestration test (stubbed fine-tune). check.sh fast lane green (242 passed, 2 skipped,
  3 deselected); ruff+mypy clean; CLI --help smoked. GPU re-test owed (Kaggle, on a COPY of the
  results): `cp data/cp24_proxy_rank.json …/cp24_warmstart.json; python -m eval.proxy_rank
  --reset-proxy --head-weights <gate-pt> --freeze-head --no-full --device cuda --imgsz 640 --batch 16
  --out …/cp24_warmstart.json` → τ≥0.7 & Δ≤0.005 ⇒ CP 2.4 CLOSES; miss ⇒ run --diagnose-full. Donor:
  gate yolo11n-pose freezes cleanly; COCO → drop --freeze-head. Original cp24_proxy_rank.json
  untouched. See procedure.md "CP 2.4 — repair: head warm-start".

  **Plan pivot 2026-07-05 (no checkpoint advance; current stays 4.1):** Phases 5–7 RE-SCOPED
  (user decision, AskUserQuestion): supernet expansion + re-search DESCOPED → D3 RESOLVED
  (descoped). Why: the old Phase-5/6 DoDs were ImageNet-framed (pre-dated the D1 pose pivot);
  the Phase-6 expanded-supernet fine-tune is infeasible on free-tier GPUs ("fine-tune budget is
  the bottleneck" — the plan's own note; OFA ≈ 1,200 V100-h); decisive: warm-head proxy σ
  (0.005–0.025) exceeds the de-noised frontier's top-cluster gaps (~0.014) → an enriched space
  is UNRANKABLE by the oracle (CP 3.5's winner's curse was this saturation biting). New content
  (PROJECT_PLAN.md rewritten, numbering kept): Phase 5 = graft-interface ablation → winner-v1.5
  (the graft is NECK-LESS with random-init adapters — the accuracy seam; Net2Wider adapter init
  + ZeroGatedTopDownNeck, warm-head proxy + full-FT top-2, ceiling-first pick); Phase 6 =
  DepGraph structured pruning → winner-v2 (measured-only latencies); Phase 7 = recipe-parity
  training (the honest baseline CP 8.3 must beat — the 0.841 vs 0.877 gap is recipe-confounded);
  Phase 4 kept, CP 4.4 re-aimed at the graft seam. OWED FIRST (Stage 0): the grafted winner has
  NEVER been benched end-to-end — 11.208 ms is the backbone-blocks-only LUT sum vs the baseline's
  full-network 12.755 ms (pose stem/adapter/head offset unmeasured; the classifier offset file
  does not apply). Export grafted + backbone-only ONNX, bench on the Nano (mode 0!), write
  data/pose_stem_head_offset.json, additively re-stamp winner.json with the honest e2e speedup;
  fallbacks [2,2,4,3,2]/[2,4,3,4,4] benched in the same session. See procedure.md "Plan pivot".

  **Phase 3b CLOSED 2026-07-07 (user-directed side run; no checkpoint advance — current
  stays 5.3):** the honest-ceiling re-search (BO under sum<=7.16ms == e2e<=baseline 12.75ms,
  2 seeds, 42 unique evals) CONFIRMS the null trade the frontier re-scoring exposed: the
  feasible band degenerates to the min-depth corner (41/42 evals d=[2,2,2,2,2]) and tops out
  at proxy 0.4965 (pre-registered expectation 0.52-0.56 — a recorded miss) vs V3's measured
  0.6287. No pick made (de-noise/e2e-bench obligations moot — noise cannot close a 0.13 gap).
  This retires the "what if the search had used the honest cost from day one?" objection.
  Artifacts: data/phase3b_honest_search.json + data/hs_bo_cache_r640.seed{0,1}.bo.jsonl.
  See procedure.md "Phase 3b CLOSED". NEXT unchanged: user framing decision, then CP 5.3.

  **Plan amendment 2026-07-07 — dense-family arm (A+B1+B2; no checkpoint advance, current
  stays 5.3):** user decisions after the similar-cases briefing: keep the arc (A) + add the
  pruned-BASELINE control ladder (B1 = CP 6.2-B, prune/prune_baseline.py) + the yolo11-pose
  scaling search (B2 = Phase 3c, search/dense_family.py; PROJECT_PLAN.md sections added).
  THREE Kaggle accounts run in parallel (push.sh KACCT/KMODE): acct1 CP 5.3 v3pan+v2topdown
  full-FTs; acct2 prune_baseline ladder; acct3 dense_scaling wave 1 (6 candidates incl. the
  from-scratch ctrl_n recipe control). All latencies measured-only (next Nano session benches
  everything); single-seed results owe CP 3.5 de-noise discipline before any pick. See
  procedure.md "Plan amendment — the dense-family arm".

  **ALL THREE CAMPAIGNS IN 2026-07-08 (no checkpoint advance; current stays 5.3):**
  - acct1 CP 5.3: v2topdown 0.846 / v3pan 0.842 (100ep, from-scratch bare-AdamW, seed 0).
  - acct2 CP 6.2-B: donor 0.887; ladder r15/r30/r45 = 0.834/0.790/0.809 (39/58/66% params).
    NON-MONOTONIC (r45>r30) ⇒ recovery-noise-bound, de-noise before CP 6.3 pick.
  - acct3 CP 3c.1: yolo11 depth mult is DEGENERATE below n (C3k2 n-floor) → wave = a 3-point
    WIDTH curve (w0.25/0.20/0.15 = 0.854/0.839/0.815); ctrl_n from-scratch 0.854 vs pretrained
    0.877 ⇒ COCO-pretrain+recipe worth only ~2.3pts (re-frames winner-v1's "deficit").
  All four families now cluster 0.79-0.85 from-scratch → LATENCY, not accuracy, separates them.
  Two Kaggle prune bugs fixed en route (rc=137 OOM: prune/yolo_tp_prep.py splits C2f chunks +
  keeps C2PSA dense, trace@128; rc=1: load_baseline_model resets the donor's CPU-cached
  criterion + dict args). SOLE remaining blocker to the cross-family verdict = ONE Nano bench
  session (winner-v1.5 e2e both necks + 3 pruned ONNX + 6 dense ONNX + deferred riders). See
  procedure.md "CP 6.2-B" / "CP 3c.1".

  **OFA-ResNet50 screen 2026-07-10 (no checkpoint advance; current stays 5.3):** the deferred
  "different supernet?" gate is CLOSED by measurement. R50 is the only pretrained+samplable+dense
  alternative to MBv3; its SMALLEST subnet backbone measures 16.17ms fp32 @640 on the Nano
  (mode 0) — already 1.27x the whole 12.75ms baseline e2e, 1.82x the 8.91ms backbone budget,
  before the pose head. Whole space 16->58ms => INFEASIBLE, D3 descoping now measured not
  estimated. Mechanism: MBv3 lost memory-bound (~0.30 TFLOP/s), R50 loses compute-bound (15.7
  GFLOPs floor = 2.4x yolo11n at a hardware-efficient 0.97-2.10 TFLOP/s). expand/screen_r50.py,
  models/screen_r50/README.md + screen_r50_result.json. See procedure.md "OFA-ResNet50 screen".

  **Prune-the-graft screen 2026-07-10 (no checkpoint advance; current stays 5.3):** the "prune the
  OFA to reduce latency?" gate. DepGraph (prune/prune_graft.py) on the winner graft at channel
  ratios 0.2/0.4/0.6, e2e ONNX @640, benched same-session (mode 0, fp32+fp16). Pruning cuts latency
  monotonically but in fp16 (deploy) only r=0.60 = 84% param sparsity beats the 7.75ms baseline
  (6.58ms); fp32 needs r>=0.40 (64%). Every rung already meets 60 FPS. 84%-gutted from a 0.61 proxy
  => accuracy-dominated by the dense arm (prune_base_* beats baseline fp16 at 0.83+ mAP). Open: CP
  6.2 recovery-train r=0.60 (prior: loses). prune/screen_prune_graft.py, models/screen_prune_graft/.
  See procedure.md "Prune-the-graft latency screen".

  **CP 6.2-G CLOSED 2026-07-11 (no checkpoint advance; current stays 5.3):** the pruned-graft
  recovery train (Kaggle acct1 v23, prune-then-TRAIN 100ep bare-AdamW): r40 = 0.8163 @ 1.07M
  (measured 11.81/8.41 ms), r60 = 0.7589 @ 0.49M (9.01/6.58 ms) vs anchor 0.841. Retention
  crushes the crater-prior (no cliff, mAP50 >= 0.896; r40 edges dense w13 per-param) BUT both
  rungs are strictly dominated on the MEASURED frontier (models/README.md — the 2026-07-08
  bench; gap ~+7-8 pts at matched latency). Screen prior confirmed; floor-pruner-config caveat
  recorded (uniform/magnitude/one-shot/no-KD => lower bounds). USER DECISION: the
  PRUNING-AS-SEARCH program (pruning = stage 2 of the HW-aware search, symmetric across
  families; Tracks 0-5 with gates G1 topology-re-ranking + G2 pruned-proxy-fidelity guarding
  the width-aware re-search). Briefing correction recorded: the cross-family Nano bench had
  ALREADY landed 2026-07-08 — remaining Nano work is riders + champion verifications only.
  See procedure.md "CP 6.2-G CLOSED". Artifacts: models/graft_pruned/.

  **Pruning-as-search rounds 1-3 (2026-07-11; no checkpoint advance):** technique table
  CLOSED on the graft — HALP-lite (LUT-knapsack) > global_taylor > uniform > iterative >
  global_l2; HALP champion 0.8042 @ pred 10.22 fp32 / 0.7936 @ pred 8.86 (stable at 2 seeds,
  Nano verification owed). G1 fallback probes (idx3+idx11) show NO topology re-ranking under
  width => Track 5 width-aware re-search CLOSED by measurement. In flight: fairness (PB
  global_taylor r20/r35, acct1) + HALP seed 2 (acct2). Next: KD recovery (CP 8.2-early) on
  both champions. See procedure.md same-date entries.

  **Pruning-as-search rounds 4-5 (2026-07-12; no checkpoint advance):** fairness leg —
  global_taylor does NOT transfer to the trained dense donor (r20 -0.4, r35 -2.3 vs uniform);
  KD twins — graft +0.84/+0.85 (PASS), dense -0.71/-0.26 (FAIL) => technique gains are
  training-state-dependent (measured twice, both directions). HALP de-noise triple closed:
  0.8064+-0.0017 / 0.7954+-0.0033. Graft champion = halp_10p4+KD 0.8126 (pred 10.22 fp32);
  dense champion unmoved = prune_base r20 0.8381 (measured 9.52/5.91). BLOCKED ON: one Nano
  session (champion graphs) -> cross-family verdict -> CP 6.3 user pick.

  **STAGE-3 ORACLE IN (2026-07-13; no checkpoint advance, current stays 5.3):** the device-
  native per-stage-width search WON. Winner s31-40-40-40-13 = 0.8786 mAP50-95 @ 2.90M / pred
  9.10 fp32 — BEATS the pretrained yolo11n baseline (0.877) FROM SCRATCH and prune_base r20
  (0.8381) by +4.0 pts at r20-class predicted latency. Proxy top-1 == oracle top-1 (search
  validated end-to-end). Discovery: wide P3/P4 stages + gutted stage-5 tail (over-provisioned
  for single-class gate pose) — allocation the 1-knob curve + DepGraph ladder couldn't express.
  TWO GATES before winner-v2: (1) seeds {1,2,3} de-noise LAUNCHED (winner's curse); (2) Nano
  MEASURED-latency bench of finalist ONNX (HALP lesson). Then winner-v2 pick vs r20 on measured
  axes -> Phases 7-9. See procedure.md "STAGE-3 ORACLE ROUND" + models/dense_nas/.

  **Stage-3 DE-NOISE CLOSED (2026-07-13; no checkpoint advance):** top-3 at seeds {1,2,3}.
  Winner's curse caught (3rd time): seed-0 leader s31 (0.8786) was +0.0085 biased AND noisiest
  (sd 0.0064). Robust winner = s39-40-38-38-14 @ 0.8709 +-0.0012 (2.77M, map50 0.9438).
  Honest verdict: MATCHES pretrained baseline 0.877 from scratch (-0.006), does NOT beat it
  (earlier claim was seed-0-biased); +3.3 over prune_base r20. Wide-P3P4/gutted-tail finding
  is seed-independent. LAST GATE: Nano measured-latency bench of s39/s31 ONNX (pred 9.55/9.10
  fp32) -> winner-v2 pick vs r20 -> Phases 7-9. See procedure.md "Stage-3 DE-NOISE CLOSED".

  **WAVE-2 CLOSED (2026-07-13; no checkpoint advance):** recalibrated re-search (physical
  act_mbytes fence + constrained box) trained 14 feasible candidates; best proxy 0.761 < ctrl_n
  0.7684, projects to ~0.839 @ 6.94 fp16 = DOMINATED by dense w22 (0.845 @ 6.95). The feasible
  per-stage search CONVERGES to the dense frontier, no dominator; wave-1's wide-feature winners
  were a fence artifact. Both pruning + width-search reach the same measured Pareto boundary
  {r20..w22..w25}. Search program CLOSED. Remaining lever to beat baseline = recipe-parity + KD
  (Phases 7-8) on a frontier point, NOT more search. See procedure.md "WAVE-2 VERDICT".

  **PIVOT 2026-07-15 (no checkpoint advance; current stays 5.3):** user constraint — the
  deployable model must be NAS-born (not YOLO / pruned YOLO). "Beat 0.877 at n's latency" and
  "n<->s Pareto band" are CLOSED by the existing record (memory-bound law: pure-OFA fp16 floor
  8.19 ms > 7.75 baseline; band height 0.0045 < 0.010 validator-config noise on the 140-img
  val). Direction (AskUserQuestion): (WS1) close winner-v2-OFA on the Orin — fresh v2_act292
  (the l4_ Lightning result 0.7637 is the POISONED cross-platform resume, discarded; the Colab
  w1a restart died preempted) + minact_arch+u30 probe + champion de-noise seeds {1,2}, ALL on
  the AGX (jetson/ MODE=prune_recover + PG_* selectors, commit d3dbd52), then ONE Nano session
  (fp32 + median-of-3 fresh-timing-cache fp16), stamp state/winner_v2_ofa/winner.json;
  (WS2) NEW Phase 10 — MCU retarget at a GAP8-class nano-drone target (D5 ACTIVATED;
  sim-first NNTool/AutoTiler/GVSOC oracle; bar = on-device downscaled int8 yolo11n +
  published nano-gate nets; new module dir mcu/). ALL training on the AGX for now (cloud
  pull-only; Nano measurement-only). Incumbent champion = r50_gtay no-KD 0.7947 @ measured
  10.23/7.48. See procedure.md "PIVOT 2026-07-15" + PROJECT_PLAN.md Phase 10.

  **BEAT-N PROGRAM OPENED 2026-07-18 (no checkpoint advance; current stays 5.3):** user
  goal = NAS-born net beating BOTH baseline bars (<12.74 fp32 / <7.75 fp16 @640) at
  de-noised mAP >= 0.83. Boundary re-framed (AskUserQuestion): s39 searched-dense lineage
  RE-ADMITTED as NAS-born + OFA-graft; new non-YOLO space not selected. Arms S (prune s39
  via act-fenced specs, PRUNED-currency law LOO 0.6%/1.2% — the dense act currency splits
  at the yolo_tp_prep rewrite), N (necked pruned graft — neck ~6-9 MB act, near-free),
  K (feature-KD at head-input taps), R (recipe-lite: cos-lr/EMA/close-mosaic). Specs
  s39d_act{240,252,259,264} + v2_topdown_act{307,314} + v2_pan_act307 emitted by honest
  CPU probes; both train paths CPU-smoked green. Wave-1 LIVE: asilarnous (s39d_act252,
  100ep, no KD) + asilarnous47 (both topdown specs, taylor+KD). Owed: weight-free Nano
  pre-bench of all 7 probe graphs (scripts/bench_beatn_probes.sh) -> wave-2 pick;
  champion de-noise seeds {1,2} before any claim. AGX access lost 2026-07-18 (A3 moved
  to Kaggle/owais). See procedure.md "BEAT-N PROGRAM OPENED".

  **BEAT-N PROGRAM CLOSED 2026-07-19 — WON (no checkpoint advance; current stays 5.3):**
  champion s39d_cap_a_rl (s39 -> spec-prune 1.8M -> 100ep recipe-lite, no KD) = de-noised
  0.8721 +/- 0.0051 @ measured 11.59 fp32 / 6.765 fp16 (baseline 0.8774 @ 12.74/7.75 ->
  -9.0%/-12.7% at -0.5pt); alternate s39d_act252_rl 0.8657 @ 10.74/6.407. All bars passed
  with margin; stamp = state/winner_beatn/winner.json + models/README.md rows + procedure.md
  "BEAT-N PROGRAM CLOSED". Graft arm retired by the weight-free pre-bench (all necked specs
  fail fp16). Successor thread: CP 10.3 MCU-native search prep (cycle oracle live; Gate 1
  recipe-parity = negative -5pts; Gate 2a proxy-fidelity @160 pending -> search go/no-go).

  **CP 10.3 CLOSED 2026-07-20 — MCU-NATIVE SEARCH WON (no checkpoint advance; current stays
  5.3):** a5fddcc (OFA-graft, deeper backbone d=[4,4,2,3,3] + gentle prune, topdown neck,
  res 192, 1.13M params) Pareto-DOMINATES the deployed baseline on GAP8: de-noised 0.6299 +/-
  0.0064 {0.6319, 0.6350, 0.6227} vs yolo11n@160 0.6227; 58.39M vs 59.85M sim cyc (3.0 vs
  2.92 FPS); 2.3x fewer params; every seed >= baseline. The lever was the base architecture
  (recipe-lite failed x2, KD null). Two-device result: OFA-graft family = TRADE on Orin
  (beat-n 0.8721 @ 11.59/6.77) + DOMINATION on GAP8 = the hardware-conditional-NAS spine.
  Stamp: state/winner_mcu/winner.json + models/README MCU section + procedure.md "CP 10.3
  CLOSED". Caveats: GAP8 sim cycles ranking-only (no AI-deck HW); champion weights not
  persisted (one save-run owed). Infra: mcu/cycle_oracle.py, mcu/mcu_screen.py,
  eval/candidate_proxy.py, kaggle MODE=candidate_proxy/proxy_rank.

  **CP 10.3 SILICON — GAP8 DOMINATION CONFIRMED ON REAL HARDWARE 2026-07-23 → RESEARCH
  PROGRAMME COMPLETE (terminal; the pivot's WS1+WS2 are both closed WON).** a5fddcc went from
  GVSOC sim to the real Crazyflie AI-deck GAP8 (new firmware mcu/board/net_bench): on silicon
  it beats the deployed baseline by 15.0% on latency (55.97M vs 65.83M cluster cyc; 319.8 vs
  376.2 ms; +17.6% FPS) — 6× the sim's 2.4% edge. The baseline (yolo11n, C2PSA attention) runs
  +10% MORE cycles on real HW than GVSOC modeled while the graft (depthwise MBv3) runs -4%
  FEWER → real silicon WIDENS the depthwise family's advantage. Sim was ranking-only by design
  and behaved so: big separations right, the near-tie 863c-vs-a5fddcc mis-ordered (863c 52.35M
  is 6.5% fewer on silicon → a trade, not a domination) — the winner is unaffected
  (accuracy-first pick). The two-device hardware-conditional-NAS result now stands ON
  MEASUREMENT at both ends: OFA-graft family = TRADE on the Orin (beat-n 0.8721 @ 11.59/6.77
  ms) + DOMINATION on the GAP8 (a5fddcc 0.6299 mAP @ 15% fewer real cycles). Stamp:
  state/winner_mcu/winner.json (silicon_measured block) + models/README.md + procedure.md
  "CP 10.3 SILICON". Bring-up cost: six toolchain gaps + a flash-size wall (STRIP_WEIGHTS →
  540 KB radio-flashable image) + a CL-clock boot hang, all baked into mcu/board/.

  **What "done" means from here (2026-07-23, user-confirmed):**
  1. The RESEARCH is complete — terminal, defensible, silicon-confirmed; the numbered checkpoint
     path terminates at CP 10.3.
  2. The TFM WRITE-UP (the deliverable) is tracked in a SEPARATE repo — NOT this one.
  3. Two non-blocking in-repo riders remain: (a) persist a5fddcc's champion WEIGHTS (one
     save-run; candidate_proxy is only an eval runner) and (b) one REAL-WEIGHTS GAP8 run for a
     headline absolute — the ranking + the 15% ratio are robust to it (int8 cycle/DMA counts are
     data-independent), so the silicon claim already holds; these only upgrade "sim/stub" to
     "measured/real weights".
  4. The old Orin deployment phases (7 recipe-parity / 8 KD / 9 TRT bundle) were SUPERSEDED by
     the 2026-07-15 NAS-only pivot, not failed: KD was tried (beat-n K arm → null-to-negative),
     recipe-lite was the decisive +5pt lever (beat-n R arm), and a shipped TRT bundle was out of
     the final NAS-demonstration scope. Recorded as scope, not silently-open checkpoints.
  5. Standing limitation (both devices): the GAP8 champion dominates but at 2.7–3.8 FPS, below
     the 15–30 FPS racing bar — the deployment gap, stated honestly.
